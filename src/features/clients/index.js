import * as actions from './actions';
import * as types from './actionTypes';
import * as models from './models';
import * as reducers from './reducers';

export default { actions, types, models, reducers };